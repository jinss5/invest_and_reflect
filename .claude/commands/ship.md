Run pre-push checks and push to origin main.

## Steps

1. Run `npm run lint` — if it fails, stop and fix the errors before continuing.
2. Run `npm run build` — if it fails, stop and fix the errors before continuing.
3. Run `git push origin main`.
